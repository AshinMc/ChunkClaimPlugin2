name: CCP
version: '${version}'
main: org.ashin.chunkClaimPlugin2.ChunkClaimPlugin2
api-version: '${apiVersion}'
softdepend: [WorldGuard, WorldEdit, PlaceholderAPI]
commands:
  claimchunk:
    description: Claim the chunk you are standing in with an optional name
    usage: /<command> [name]
    permission: ccp.claim
  unclaimchunk:
    description: Unclaim a chunk (by name removes entire group, no args unclaims current chunk)
    usage: /<command> [name]
    permission: ccp.unclaim
  checkchunk:
    description: Check the ownership and claim name of the chunk you are standing in
    usage: /<command>
    permission: ccp.check
  infochunk:
    description: Get a list of your claimed chunk groups
    usage: /<command>
    permission: ccp.info
  visualizechunk:
    description: Visualize your claim borders (by name or all)
    usage: /<command> [name]
    permission: ccp.visualize
  chunkexpand:
    description: Expand an existing named claim by adding the current chunk
    usage: /<command> <name>
    permission: ccp.expand
  chunklang:
    description: View or set your language/locale
    usage: /<command> [get|list|set <locale>]
    permission: ccp.lang
  chunksettings:
    description: Open the chunk settings GUI
    usage: /<command>
    permission: ccp.settings
  chunktp:
    description: Teleport to a chunk by claim name
    usage: /<command> <name>
    permission: ccp.teleport
  chunkadmin:
    description: Open the admin settings GUI
    usage: /<command>
    permission: chunkclaim.admin
permissions:
  ccp.claim:
    description: Allows claiming chunks
    default: true
  ccp.unclaim:
    description: Allows unclaiming chunks
    default: true
  ccp.check:
    description: Allows checking chunk ownership
    default: true
  ccp.info:
    description: Allows viewing your claim groups
    default: true
  ccp.visualize:
    description: Allows visualizing claims
    default: true
  ccp.expand:
    description: Allows expanding claims
    default: true
  ccp.lang:
    description: Allows changing player language
    default: true
  ccp.settings:
    description: Allows using the chunk settings GUI
    default: true
  ccp.teleport:
    description: Allows teleporting to chunks
    default: true
  ccp.flag.*:
    description: Allows toggling all claim flags in settings GUI
    default: true
  ccp.flag.mob-griefing:
    description: Allows toggling mob-griefing flag
    default: true
  ccp.flag.mob-spawning:
    description: Allows toggling mob-spawning flag
    default: true
  ccp.flag.mob-entry:
    description: Allows toggling mob-entry flag
    default: true
  ccp.flag.mob-protection:
    description: Allows toggling mob-protection flag
    default: true
  ccp.flag.explosions:
    description: Allows toggling explosions flag
    default: true
  ccp.flag.pvp:
    description: Allows toggling pvp flag
    default: true
  ccp.flag.fire-spread:
    description: Allows toggling fire-spread flag
    default: true
  ccp.flag.greeting-title:
    description: Allows toggling greeting-title flag
    default: true
  ccp.flag.interact-chest:
    description: Allows toggling chest interaction flag
    default: true
  ccp.flag.interact-furnace:
    description: Allows toggling furnace interaction flag
    default: true
  ccp.flag.interact-stonecutter:
    description: Allows toggling stonecutter interaction flag
    default: true
  ccp.flag.interact-door:
    description: Allows toggling door interaction flag
    default: true
  ccp.flag.interact-redstone:
    description: Allows toggling redstone interaction flag
    default: true
  chunkclaimprotection.bypass:
    description: Allows bypassing chunk protection checks
    default: op
  chunkclaim.admin:
    description: Allows access to the admin settings GUI
    default: op
  chunkclaim.teleport:
    description: Allows using the Teleport action from the GUI
    default: op